# ==============================================================================
# 量化导航仪后端服务 - 特定依赖
# ==============================================================================
# 注意：主要依赖由根目录的 requirements.txt 管理
# 此文件仅包含后端特定的依赖或版本覆盖
# ==============================================================================

# 后端特定依赖（如果需要与根目录不同版本）
# 例如：
# fastapi==0.100.1  # 如果后端需要特定版本
# uvicorn==0.23.2   # 如果后端需要特定版本

# 当前后端使用根目录的统一依赖管理
# 如需添加后端特定依赖，请在此处添加
