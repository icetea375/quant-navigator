{
  "name": "@quant-navigator/api",
  "version": "1.0.0",
  "description": "量化导航仪API应用入口",
  "private": true,
  "scripts": {
    "dev": "cd ../../packages/backend-python && python -m uvicorn src.main:app --reload",
    "build": "echo 'Python package - no build needed'",
    "start": "cd ../../packages/backend-python && python -m uvicorn src.main:app --host 0.0.0.0 --port 8000"
  },
  "dependencies": {
    "@quant-navigator/backend-python": "workspace:*"
  }
}
